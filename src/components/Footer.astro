---
import { SettingsPanel } from './SettingsPanel';

const currentYear = new Date().getFullYear();
---

<footer transition:persist class="flex items-center justify-between px-4 py-6 md:px-8">
  <p class="animate-fade-in text-xs text-white/60" style="--delay: 340ms;">© 2013-<span id="current-year">{currentYear}</span> carterdea.com</p>

  <div class="animate-fade-in" style="--delay: 380ms;">
    <SettingsPanel client:visible />
  </div>
</footer>

<script>
  // Update copyright year client-side to always be current
  const yearSpan = document.getElementById('current-year');
  if (yearSpan) {
    yearSpan.textContent = String(new Date().getFullYear());
  }
</script>
